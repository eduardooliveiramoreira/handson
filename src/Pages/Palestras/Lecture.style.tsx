import {makeStyles} from "@material-ui/core";

export const lecturePageStyle = makeStyles(theme => ({
    page: {
        width: '100%',
        justifyContent: 'center',
        marginTop: -100
    },
}));
